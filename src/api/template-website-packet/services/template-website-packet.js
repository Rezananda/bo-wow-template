'use strict';

/**
 * template-website-packet service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::template-website-packet.template-website-packet');
