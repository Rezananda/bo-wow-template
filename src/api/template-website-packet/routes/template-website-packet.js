'use strict';

/**
 * template-website-packet router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::template-website-packet.template-website-packet');
