'use strict';

/**
 * build-deck-request service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::build-deck-request.build-deck-request');
